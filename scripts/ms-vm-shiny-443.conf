<VirtualHost *:443>
    ServerName mscf-vm.biochem.mpg.de

    SSLEngine on
    SSLCertificateFile /etc/ssl/certs/mscf-cert.pem
    SSLCertificateKeyFile /etc/ssl/private/2026_private-key.pem
    SSLCertificateChainFile /etc/apache2/certificate/2026_Cert_binary.cer

    ProxyPreserveHost On
    ProxyRequests Off
    RequestHeader set X-Forwarded-Proto "https"

    RewriteEngine On
    RewriteRule ^/$ /ms-app/ [R=302,L]

    ProxyPass /ms-app/ http://127.0.0.1:3838/ms-app/
    ProxyPassReverse /ms-app/ http://127.0.0.1:3838/ms-app/

    <Location /ms-app/>
        AuthType Basic
        AuthName "LDAP Authentication"
        AuthBasicProvider ldap
        AuthLDAPURL "ldaps://ldapserv1.biochem.mpg.de:636/dc=biochem,dc=mpg,dc=de?uid?sub?(objectClass=posixAccount)"
        Require valid-user

        RequestHeader unset X-Remote-User
        RequestHeader unset X-Forwarded-User
        RequestHeader edit Cookie "(^|;\\s*)auth_user=[^;]*" ""
        RequestHeader set X-Remote-User "%{REMOTE_USER}e"
        RequestHeader set X-Forwarded-User "%{REMOTE_USER}e"

        RewriteEngine On
        SetEnvIfExpr "%{QUERY_STRING} =~ /(?:^|&)auth_user=([^&]*)/" QS_AUTH_USER=$1
        RewriteCond %{REMOTE_USER} (.+)
        RewriteRule ^ - [E=AUTH_CANONICAL_USER:%1]
        RewriteCond %{IS_SUBREQ} =false
        RewriteCond %{REQUEST_URI} ^/ms-app/?$
        RewriteCond %{ENV:AUTH_CANONICAL_USER} !^$
        RewriteCond expr "%{ENV:QS_AUTH_USER} != %{ENV:AUTH_CANONICAL_USER}"
        RewriteRule ^ /ms-app/?auth_user=%{ENV:AUTH_CANONICAL_USER} [R=302,L,NE,QSD]
    </Location>

    RewriteCond %{HTTP:Upgrade} websocket [NC]
    RewriteCond %{HTTP:Connection} upgrade [NC]
    RewriteRule ^/ms-app/(.*) "ws://127.0.0.1:3838/ms-app/$1" [P,L]

    LogLevel warn
    ErrorLog ${APACHE_LOG_DIR}/ms-app-ssl-error.log
    CustomLog ${APACHE_LOG_DIR}/ms-app-ssl-access.log combined
</VirtualHost>
